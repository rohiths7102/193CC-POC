# Copy to .env.production on the server. NEVER commit real values.
DATABASE_URL="postgresql://postgres:${DB_PASSWORD}@db:5432/membership?schema=public"
DB_PASSWORD="generate-a-strong-one"
SESSION_SECRET="openssl rand -hex 32"
APP_URL="https://members.193countriesconsortium.com"

# Go-live provider keys — email AND the Companies House check go real the
# moment their tokens are set; payments/e-sign adapters are the remaining
# integration work (see DEPLOYMENT.md).
POSTMARK_SERVER_TOKEN=""
MAIL_FROM="membership@193countriesconsortium.com"
STRIPE_SECRET_KEY=""
GOCARDLESS_ACCESS_TOKEN=""
ESIGN_API_KEY=""
# Free key: https://developer.company-information.service.gov.uk
COMPANIES_HOUSE_API_KEY=""
